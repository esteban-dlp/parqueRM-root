PRAGMA foreign_keys = ON;
PRAGMA journal_mode = WAL;
PRAGMA synchronous = NORMAL;
PRAGMA busy_timeout = 5000;

/* ============================================================
   01_create_database.sql
   SQLite version.
   SQLite has no CREATE DATABASE step; the database is the .db file.
   This file intentionally only configures connection pragmas.
   ============================================================ */
